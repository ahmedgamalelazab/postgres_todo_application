import 'package:flutter/material.dart';

class UserTodoScreenAfterSignUpNotVerifiedYet extends StatelessWidget {
  const UserTodoScreenAfterSignUpNotVerifiedYet({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
